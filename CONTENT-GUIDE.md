# Agent / LLM 高信号简历内容指南

这份指南研究的是：为什么一份应届生简历可能获得远高于普通校招候选人的市场定价，以及普通候选人如何学习它的证据组织方式。

需要先划清边界：简历文件名或转述不能证明候选人实际获得了某个 offer，更不能证明具体薪资。本指南只分析简历呈现出的竞争力信号，不承诺求职结果。

## 1. 真正值钱的不是排版

高定价来自多个稀缺信号同时成立，而不是蓝色标题、粗体或两页纸：

| 信号 | 招聘者看到的含义 | 有效证据 |
| --- | --- | --- |
| 公开影响力 | 能力经过陌生人和真实用户检验 | stars、contributors、release、真实用户、下游采用 |
| 系统所有权 | 能独立负责复杂结果，而非执行零散任务 | owner 边界、架构决策、核心代码、上线责任 |
| 技术深度 | 理解 Agent 的运行机制，不只会调 API | 调度、context、sandbox、trace、eval、verifier |
| 算法闭环 | 能把模型优化变成可重复实验 | 数据、训练、baseline、metric、消融、failure |
| 商业结果 | 技术能转化为效率、成本或增长 | 采用率、留存、转化、耗时、成本、稳定性 |
| 持续产出 | 不是一次性包装出来的项目 | 跨时间的 commit、文章、版本、用户反馈 |

单独写“熟悉 LangChain”几乎没有溢价。写清楚“在什么约束下做了什么关键决策，并用什么证据证明结果”才有。

## 2. 原简历最值得学习的七点

### 2.1 第一屏先给稀缺证据

姓名之后立刻出现岗位定位、开源角色、项目影响力和最强经历。招聘者无需读完整份简历，就能判断候选人是否值得继续看。

正确顺序是：

```text
岗位定位 -> 最强公开证据 -> 核心经历 -> 其他补充
```

教育背景重要，但如果学校不是最强信号，不必机械地占据第一屏最大面积。

### 2.2 用 owner 描述责任边界

“参与项目”无法区分旁观者和负责人。高信号表达需要回答：

- 哪个目标由你负责？
- 哪些关键决策由你做？
- 哪些核心模块由你实现或 review？
- 失败时谁定位，结果由谁验收？

只有确实负责时才使用 `owner`、`主导` 或 `从 0 到 1`。否则写具体贡献，可信度更高。

### 2.3 技术名词都连接到机制

弱写法：

```text
使用 LangGraph、RAG、MCP 和多智能体完成复杂任务。
```

高信号写法：

```text
将固定工作流改为 Lead Agent 统一决策、sub-agent 隔离执行；
通过按需 skill 加载与 checkpoint，将长任务上下文占用降低 X%，
中断恢复成功率提升至 Y%。
```

框架名只是索引。面试官真正关心的是架构、约束、失败模式和取舍。

### 2.4 项目形成完整因果链

每个重点项目尽量覆盖：

```text
问题/约束 -> 我的责任 -> 关键机制 -> 验证设计 -> 技术结果 -> 业务结果
```

不必把六部分拆成六个小标题，但不能只写中间的“做了什么”。

### 2.5 指标不只衡量准确率

高价值工程师会同时管理多个目标：

| 维度 | 指标示例 |
| --- | --- |
| 质量 | 任务成功率、pass@k、MRR、人工偏好率 |
| 效率 | P95/P99 延迟、token、GPU 小时、单任务成本 |
| 规模 | 请求量、用户量、数据量、并发量 |
| 可靠性 | 恢复成功率、超时率、循环率、回归缺陷数 |
| 业务 | 采用率、留存、转化、报告耗时、人工返工率 |
| 外部验证 | stars、contributors、下游集成、论文与引用 |

同一项目通常选择 2--3 个最能证明目标的指标，不要把仪表盘全部搬进简历。

### 2.6 研究、工程和业务互相验证

研究说明能提出和验证方法，工程说明能把方法做成稳定系统，业务说明系统有人使用，开源说明结果能接受外部审查。四者同时出现时，候选人的定价不再只参考“应届生工作年限”。

### 2.7 每个强结论都有下钻入口

简历不是技术报告，而是证据索引。重点经历至少准备一个可访问入口：

- GitHub 仓库或关键 PR；
- 在线 Demo 或录屏；
- 技术报告、论文或实验表；
- 脱敏 trace、failure case 与复现步骤。

没有公开链接时，也要能在面试中展示脱敏证据并解释个人贡献边界。

## 3. Bullet 写作公式

推荐公式：

```text
在[场景与约束]下，作为[责任角色]，[关键决策与实现]；
基于[数据集/流量/统计窗口]对比[基线]，使[技术指标]从 X 到 Y，
并带来[业务结果或外部证据]。
```

一个 bullet 只承载一个主结论。若一句话同时包含四套架构、六个指标和三个结果，应拆分或删除次要信息。

### 从弱到强

弱：

```text
负责多智能体和 RAG 系统开发，效果较好。
```

中：

```text
实现规划、检索和总结三个 Agent，提升复杂问答效果。
```

强：

```text
负责多跳问答 Agent 的架构与评测，将单 Agent 拆为规划、检索和总结节点；
在 X 条固定测试集上相对 ReAct 基线，任务成功率由 X% 提升至 Y%，
平均工具调用数下降 Z%。
```

## 4. 数字的四连问

写入任何数字前，确保能回答：

1. 分母是什么，样本量多大？
2. 基线是谁，同口径吗？
3. 数据来自哪个环境和统计窗口？
4. 哪些因素可能同时影响结果，你做过什么消融或对照？

无法回答时，改成可核验的规模描述，或先补实验。不要为了“量化”制造无法解释的精确数字。

## 5. 三类候选人的模块替换

`resume-agent.tex` 提供一条主线，但不要求每个人都拥有相同经历。

| 候选人类型 | 第一证据 | 第二证据 | 应删除的内容 |
| --- | --- | --- | --- |
| 开源型 | 核心仓库角色与采用数据 | 关键架构和 release | 无贡献边界的 stars 罗列 |
| 科研型 | 问题、方法和强基线结果 | 代码、消融、失败分析 | 只写“投稿中”的空成果 |
| 业务型 | 负责范围和线上结果 | 稳定性、成本、采用率 | 无法脱敏验证的宏大叙事 |

没有头部开源经历时，不要伪造。课程项目也可以通过公开 benchmark、复现实验、代码质量和真实用户反馈建立证据链。

## 6. 不应照抄的地方

### 6.1 不使用 A3 密排

源 PDF 是两页 A3。它在屏幕预览中显得信息丰富，但不利于常见招聘系统预览、打印和快速扫描。开源模板改为两页 A4，并通过删减内容控制页数，而不是继续缩小字号。

### 6.2 不把长段落当成技术深度

细节多不等于信号强。保留能证明判断力的机制、取舍和结果，删除实现流水账、重复背景与可从项目名推断的信息。

### 6.3 不让目标岗位与证据错位

如果最强证据是 Agent Harness、轨迹后训练和评测，主定位应是 Agent / LLM 算法或系统工程师。投多模态岗位时，需要补充视觉编码、跨模态对齐、数据构造、训练与多模态 benchmark，而不是只在标题里加入“多模态”。

### 6.4 不堆无法验证的光环

年龄、公司名、头衔和奖项能吸引注意，但不能替代贡献证据。社区角色应给出项目、贡献周期和关键 PR；商业数据应能解释统计口径；论文状态应准确区分已发表、已录用和在投。

### 6.5 不使用泛化自我评价

删除“学习能力强”“熟悉前沿技术”“沟通能力优秀”。用持续贡献、跨团队推进、复现报告和实际采用证明这些能力。

## 7. 针对原简历的升级优先级

### P0：直接影响可信度与岗位匹配

1. **统一岗位定位**：当前内容更接近 Agent Infra / Agent 算法，而不是纯多模态算法；为不同 JD 分别生成版本。
2. **给每个核心数字补口径**：在作品集准备样本量、基线、窗口、环境和消融，简历正文只保留结果。
3. **明确个人贡献边界**：团队结果与个人结果分开，`owner`、`主导`、`参与`使用一致标准。
4. **改为两页 A4**：适配 ATS、招聘网站预览与打印；不要依靠极小字号维持内容总量。
5. **核验所有外部身份与成果状态**：开源角色、论文状态、奖项、用户数和商业数据都应有可出示证据。

### P1：提高十秒扫描效率

6. **每段保留 2--4 个最强 bullet**：按所有权、机制、验证、结果排序，其余进入作品集。
7. **把最强公开证据前置**：开源项目名、角色、采用数据和链接放在第一屏，不用长实习总结稀释。
8. **拆开训练与评测项目**：轨迹合成、SFT/RL 和 rubric eval 各自给出输入、关键决策和结果，避免术语混成一段。
9. **增加失败与取舍证据**：为每个旗舰项目准备一个失败样本和一个未采用方案，面试时证明判断不是事后包装。
10. **降低视觉噪声**：减少满页粗体、彩色链接和超长标题，只高亮角色、关键机制与结果。

### P2：把强简历变成完整招聘资产

11. **建立证据索引页**：为每个 bullet 关联代码、trace、实验表、截图或证明材料。
12. **准备 JD 定制版本**：Agent 系统、后训练、商业 Agent、多模态四类岗位使用不同项目顺序与关键词。
13. **补作品集短入口**：一个页面呈现架构图、核心指标、Demo、failure case 和个人贡献。
14. **准备口径一致的三种讲法**：30 秒、2 分钟和深挖版使用同一组事实与数字。
15. **删除不可持续维护的内容**：过时模型名、失效链接、无法复现的 benchmark 和无法解释的精确数字应及时更新。

## 8. 发布前自检

- [ ] 十秒内能看出目标岗位和最强证据。
- [ ] 每个项目都写清个人责任边界。
- [ ] 每个技术名词至少连接一个机制、取舍或结果。
- [ ] 每个数字能回答分母、基线、窗口和环境。
- [ ] 团队成果没有包装成个人成果。
- [ ] 论文、奖项、开源角色和商业数据状态准确。
- [ ] 重点项目有代码、trace、实验或作品集入口。
- [ ] 两页 A4，无小字堆叠、跨页断裂和失效链接。
- [ ] 技能列表只保留能深入追问或现场实现的内容。
- [ ] 针对目标 JD 调整了第一屏、项目顺序和关键词。

简历能获得高定价的根本原因，是它压缩并索引了已经存在的稀缺证据。模板只能改善证据的可见性，不能替代项目深度、公开贡献和可验证结果。
